# hello

Pure static app — plain HTML/CSS, no build. Published as-is to `…/hello/`.

```bash
npx serve apps/hello    # or just open index.html
```

Use **relative** asset paths so it works under the Pages subpath. `app.json` is
landing-page metadata and isn't published.
